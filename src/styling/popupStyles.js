const popupContent = {
    textAlign: "center",
    height: "350px",
    marginTop: "30px"
  };
  const popupHead = {
    fontWeight: "bold",
    fontSize: "22px"
  };
  
  const popupText = {
    fontSize: "15px",
    marginBottom: "20px"
  };
  
  const okText = {
    fontSize: "15px"
  };
  
  const articlePopup = {
    display: "flex",
    alignItems: 'center',
    gap: '10px',
    width: 'max-width',
    padding: '1rem'
  }
  export { popupContent, popupHead, popupText, okText, articlePopup };